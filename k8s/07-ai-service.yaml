# AI Service - Future Implementation
# This service is not currently applied and will be added in a future phase
# Uncomment and apply when AI service is ready for deployment

# apiVersion: v1
# kind: Service
# metadata:
#   name: ai-service
#   namespace: reciplore
# spec:
#   selector:
#     app: ai-service
#   ports:
#   - port: 5000
#     targetPort: 5000
#   type: ClusterIP
